# Copy to .env and set a strong password before the first start.
POSTGRES_DB=merito
POSTGRES_USER=merito
POSTGRES_PASSWORD=change-me

# Generate one VAPID key pair and keep the private key secret.
WebPush__PublicKey=
WebPush__PrivateKey=
WebPush__Subject=mailto:admin@example.com
